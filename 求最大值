#include<stdio.h>
//最大值
int main()
{
	int arr[] = { -1,-2,-3,1,2,3,4,5,6,7,8 };
	int max = arr[0];//最大值
	int i;
	int sz = sizeof(arr) / sizeof(arr[0]);//求数组个数
	for (i = 1;i < sz;i++)
	{
		if (arr[i] > max)
		{
			max = arr[i];
		}
	}
	printf("max=%d\n", max);
	return 0;
}
